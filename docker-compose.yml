services:
  hummingbot-mcp:
    build: .
    image: hummingbot/mcp-server:latest
    volumes:
      - hummingbot_mcp_config:/root/.hummingbot_mcp
    stdin_open: true
    tty: true
    restart: unless-stopped

volumes:
  hummingbot_mcp_config:
    driver: local
